doctype html
head
    meta(charset="utf-8")
    style.
        html{
            font-size:62.5%;
            font-family:Hiragino Sans GB,Helvetice,Arial;
        }
        .container{
            max-width:80rem;
            margin-left:auto;
            margin-right:auto;
            padding-top:7em;
        }

body
    #app.container
        top-header
        list-container